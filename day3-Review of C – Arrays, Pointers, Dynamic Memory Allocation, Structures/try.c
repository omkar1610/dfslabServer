
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main () 
{
   

   char *ptr[256];// array of pointers where each pointer points to a string

   char data[512];
   char str[512];
   scanf("%s",str);
   printf("%s\n", str);



   
   return(0);
}